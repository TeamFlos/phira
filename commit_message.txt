feat: 添加本地铺面文件夹管理功能

实现了完整的文件夹系统。

功能特性：
- 支持创建、重命名、删除文件夹，支持多级目录结构
- 通过铺面卡片右下角的菜单按钮在文件夹间移动铺面
- 支持自定义文件夹图标（可设置自定义图片）
- 文件夹导航功能，支持层级浏览
- 支持在指定文件夹内直接导入铺面
- 文件夹状态持久化保存
- 文件夹名称验证（防止非法字符和重名）

用户界面：
- 文件夹卡片显示在铺面列表顶部
- 进入文件夹后显示导航按钮（返回、重命名、更改图标、删除）
- 每个铺面卡片右下角显示菜单按钮，用于文件夹操作
- 提供中英文本地化支持
